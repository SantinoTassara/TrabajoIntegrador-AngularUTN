import { SetTimeFormatPipe } from './set-time-format-pipe';

describe('SetTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SetTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
